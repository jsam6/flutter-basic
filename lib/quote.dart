
class Quote {
    String text = '';

    Quote(String text) {
        this.text = text;
    }

    // Quote({String text}) {
    //     this.text = text;
    // }
    // Quote(text: 'help');

    // Quote({ this.text })
    // Quote(text: 'help');
}

